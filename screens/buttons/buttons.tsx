import { StyleSheet, View, Text, Button } from "react-native";

export default function ButtonsScreen() {
    <View style={buttonScreenStyle.container}>
        
    </View>
}

const buttonScreenStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    }

});